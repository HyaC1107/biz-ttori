# 🤖 또리 스펙업 기록 (Agent Spec & Tooling Log)

이 문서는 **Biz-Ttori** 에이전트들(젬또리, 클또리)의 성격(페르소나) 보강, 자동화 스킬 구축, 협업 규칙 변경 등 **또리 스펙업 및 AI 툴링 개선 이력**을 누적 보관하는 공식 히스토리 문서입니다. (사내 보고용 작업일지와 기재 대상을 이원화하여 관리합니다.)

---

## 📅 2026-07-27 개편 내역

### 1. 🎯 업무 중심 전환: React Native/Fullstack → AI 모델 개발
* **스펙업 내용:** 회사 신규 온보딩에 따라 워크스페이스의 업무 중심이 React Native/React/Fullstack 개발에서 **AI 모델 개발**로 전환됨(세부 스코프는 아직 미정). `CLAUDE.md`, `triggers.md`, `.claude/agents/*.md` 5종에 하드코딩돼 있던 RN/Fullstack 전용 규칙·체크리스트(Podfile/Gradle, API/DB 규칙, RN UI 체크리스트 등)를 걷어내고, 스코프에 무관한 원칙(Grounding-우선, 재현성 기록, 자격증명 보호 등)만 남긴 "TBD" 자리표시자로 교체.
  * **코더봇(`coder.md`):** 페르소나가 "실용주의 풀스택 장인" → **"실용주의 엔지니어"**로 변경.
* **지침 반영:** `CLAUDE.md` 정체성/§1 개발 규칙, `triggers.md` PR 게이트 매트릭스를 동일 기준으로 갱신.

### 2. 🩺 문서 드리프트 정리 (G-Brain)
* **스펙업 내용:** 오늘 새벽 환경 초기화로 `projects/`(FMS, close, fanbird-broadcast) 폴더가 비워졌는데도 `memory/g-brain-map.md`·`memory/api-specs.md`가 여전히 해당 프로젝트를 "🟢 활성"으로, 존재하지 않는 `specs/` 문서(slack-integration-spec 등)를 유효한 것처럼 가리키고 있던 드리프트를 발견하고 정리함(`team-rules.md` 교훈 #2 적용 사례).
* **지침 반영:** 두 문서의 해당 표를 비우고, 새 프로젝트/툴이 붙으면 그때 한 행씩 추가하도록 각주 추가.

### 3. 📦 과거 RN/Fullstack 실증 지식 보관
* **스펙업 내용:** `memory/stack.md`(RN/Fullstack 트러블슈팅)와 `_templates/rn-exception-test-checklist-template.md`(fanbird-broadcast 실기기 테스트 체크리스트)는 삭제하지 않고 각각 `memory/_archive/stack-fullstack.md`, `_templates/_archive/`로 이동해 보존. `stack.md` 본체는 AI 모델 개발용 빈 뼈대로 재작성.

---

## 📅 2026-07-02 개편 내역

### 1. 📬 양방향 우체통(inbox.md / outbox.md) 소통 체계 수립
* **스펙업 내용:** 기존 젬또리 단방향 전달 우체통(`inbox.md`)에서, 클또리가 젬또리에게 보고를 배달할 수 있는 **`outbox.md` 채널을 신설**하여 양방향 비동기 협업 우편 체계 구축.
* **지침 반영:** 젬또리는 기동 시 항상 `memory/outbox.md`를 우선 팩트체크하고 작업을 시작하도록 [.agents/AGENTS.md](file:///Users/linkcampus02/biz-ttori/.agents/AGENTS.md) 규칙에 박제.

### 2. 📝 작업일지(Daily Logs) 작성 필터 규칙 수립
* **스펙업 내용:** 작업일지에는 오직 실제 서비스 프로젝트(`close`, `fanbird`) 구현 이력만 적고, 에이전트 스펙업/툴링 관련 업데이트 이력은 이 문서(`또리스펙문서.md`)에 단독 관리하도록 작성 가이드라인 이원화 확정.
* **지침 반영:** [.agents/AGENTS.md](file:///Users/linkcampus02/biz-ttori/.agents/AGENTS.md)의 `## 📝 작업일지(Daily Logs) 작성 규칙` 조항으로 공식 반영하여 클또리/젬또리 적용.

### 3. 🧠 서브에이전트 페르소나 및 성격 주입
* **스펙업 내용:** `.claude/agents/` 내의 핵심 서브에이전트들에게 성격 섹션을 추가하여 구현 품질과 요약 정밀도를 고도화함.
  * **코더봇(`coder.md`):** 오버엔지니어링을 배제하는 **"실용주의 풀스택 장인"**
  * **라이터봇(`writer.md`):** 바쁜 팀원을 위해 리소스를 아끼는 **"미니멀리스트 요약가"**

### 4. 🩺 지브레인(G-Brain) 무결성 검사 오탐 방지 패치
* **스펙업 내용:** `gbrain-doctor.sh` 와 `heal.js` 스캔 시, 마크다운 이미지/미디어 파일 링크(`.png`, `.jpg`, `.jpeg`, `.gif`, `.pdf`)를 404 깨진 링크로 오진하는 버그를 팩트체크하여 예외 스킵 처리하도록 긴급 패치 완료.

---

## 📅 2026-07-01 개편 내역

### 1. 3대 자동화 커스텀 스킬 구축 및 검증 완료
* **`gbrain-healer`:** 깨진 위키링크 Levenshtein 유사도 기반 자동 치료 스크립트.
* **`gbrain-map-sync`:** `projects/` 생성 시 지브레인 노드 맵(`g-brain-map.md`) 테이블 자동 동기화 스크립트.
* **`inbox-cleaner`:** 임시 우체통을 오늘 자 일지 하단 아카이브로 이관 후 씻어내는 청소 스크립트.
