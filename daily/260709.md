# 2026년 07월 09일 (목) 개발 작업 일지

## 1. 🛠️ 작업 내역 요약 (Summary)
*   **안드로이드 권한 사전 고지(Prominent Disclosure) 모달 및 로직 구현**
    *   **대상 파일**: [BroadcastScreen.tsx](file:///Users/linkcampus02/fanbird-broadcast/src/screens/Broadcast/BroadcastScreen.tsx)
    *   **구현 범위**: 구글 플레이 스토어 정책을 준수하기 위해 시스템 권한 팝업 호출 전 앱 자체 설명 동의 팝업(Disclosure UI) 노출 프로세스 개발.
*   **close 웹 주문내역 컬럼 수정 범위 조율 및 메모 placeholder 기능 구현**
    *   **대상 파일**: [orderTable_style2.tsx](file:///Users/linkcampus02/close/src/Component/style/orderTable_style2.tsx), [Order_paydeil.tsx](file:///Users/linkcampus02/close/src/Page/Manage/OrderList/Component/Order_paydeil.tsx), [Order_refound.tsx](file:///Users/linkcampus02/close/src/Page/Manage/OrderList/Component/Order_refound.tsx)
    *   **구현 범위**: 택배사 및 송장번호 컬럼의 개별 수정 권한(연필 아이콘)을 회수하여 읽기 전용으로 전환하고, 메모 컬럼만 내용이 없을 시 placeholder를 제공하며 수정이 가능하도록 조율. 병합셀 flex 레이아웃과의 레이아웃 충돌 방지 조치 완료.
*   **close 웹 주문내역 컬럼 수정 범위 조율 및 메모 placeholder 기능 구현**
    *   **대상 파일**: [orderTable_style2.tsx](file:///Users/linkcampus02/close/src/Component/style/orderTable_style2.tsx), [Order_paydeil.tsx](file:///Users/linkcampus02/close/src/Page/Manage/OrderList/Component/Order_paydeil.tsx), [Order_refound.tsx](file:///Users/linkcampus02/close/src/Page/Manage/OrderList/Component/Order_refound.tsx)
    *   **구현 범위**: 택배사 및 송장번호 컬럼의 개별 수정 권한(연필 아이콘)을 회수하여 읽기 전용으로 전환하고, 메모 컬럼만 내용이 없을 시 placeholder를 제공하며 수정이 가능하도록 조율. 병합셀 flex 레이아웃과의 레이아웃 충돌 방지 조치 완료.

---

## 2. 🔍 세부 작업 상세 (Details)

### ① 앱 자체 사전 권한 고지 팝업 (Disclosure Modal View)
*   **비주얼 & 디자인**:
    *   Pretendard 폰트 및 모던한 컴포넌트 설계를 반영하여 깔끔한 모달 형태로 구현했습니다.
    *   상단에 **"카메라 및 마이크 권한 접근 안내"** 타이틀과 본문 설명을 배치하고, 선택 권한 리스트(카메라 권한, 마이크 권한)의 상세 사용 목적을 별도의 밝은 회색 테두리 상자(`disclosureItem`)로 가시성 높게 설계했습니다.
    *   하단 영역에는 동의 및 거부 버튼(`disclosureDenyBtn`, `disclosureAgreeBtn`)을 좌우 1:1 비율로 정밀 배치하고, 동의 버튼은 메인 브랜딩 칼라(`#7955f0`, 보라색)로 강조하여 통일감 있게 배치했습니다.
*   **동의 프로세스 비동기 제어**:
    *   최초 권한 여부 조회 및 방송 시작(`handleStart`) 버튼 탭 시, `Platform.OS === 'android'` 분기 처리를 통해 아직 권한을 획득하지 않은 경우 `showDisclosure` 모달이 표시되도록 구현했습니다.
    *   모달 노출 시 비동기 `Promise` 대기열 구조를 적용하고, `permissionResolveRef`에 resolve 함수를 수납하여 사용자가 "동의 및 허용"을 클릭했을 때만 실제 네이티브 SDK 권한 팝업(`IVSBroadcast.requestPermissions()`)을 동작시킵니다. "거부" 선택 시 권한 요청 없이 정상적으로 복귀(Reject) 처리됩니다.

### ② close 웹 주문내역 테이블 수정 (읽기 전용 처리 및 메모 placeholder 도입)
*   **택배사 및 송장번호 읽기 전용화**: 
    *   관리자가 화면에서 건별로 직접 수정할 수 있게 제공되던 `modify_memo` 연동 연필 아이콘(`ic_modi.png`)과 `window.prompt` 수동 변경 기능을 [Order_paydeil.tsx](file:///Users/linkcampus02/close/src/Page/Manage/OrderList/Component/Order_paydeil.tsx) 및 [Order_refound.tsx](file:///Users/linkcampus02/close/src/Page/Manage/OrderList/Component/Order_refound.tsx)에서 삭제하여, 레이아웃 깨짐 없이 순수 읽기 전용 텍스트만 렌더링되도록 수정했습니다.
*   **메모 placeholder 도입 및 스타일 차별화**: 
    *   관리자 메모가 비어있을 경우 기본값 `"-"` 대신 **`"내용을 입력해주세요."`** 가 노출되도록 `orderTable_style2.tsx` 내의 메모 keyMap 매퍼를 보완했습니다.
    *   메모는 병합셀 대상이 아니므로 병합셀 flex 구조에 어떤 영향도 미치지 않고 안전하게 렌더링됩니다. 내용이 없는 placeholder 상태일 때는 글자 색상을 연한 회색(`#999`)으로 낮추는 CSS 인라인 스타일링을 적용했습니다.

---

## 3. 🤖 클또리 작업 내역 (close 웹 — 재고 초과 주문건 기능 + 라우팅 버그 픽스)

* **[치명적 버그] 주문내역 탭 라우팅 오류 수정**: `Orderlist.tsx`의 탭별 컴포넌트 분기가 `Order_select_menu.tsx`의 실제 키(`payment-wait`/`payment-done`/`order-done`/`shipping`/`delivery-done`/`cancel`/`refund`)가 아니라 옛 키(`non-pay`/`done-pay`/`cancle`/`refound` 등)를 검사하고 있어서 **모든 탭이 항상 `Order_all`로만 렌더링**되고 있었음(20개 컬럼 통일 작업 때문에 겉보기엔 티가 안 났음). `Order_paydeil`/`Order_cancel`/`Order_refound`가 실제 라우팅되도록 키 정합.
  * 부수 버그 2건도 같이 수정: 대시보드 "주문내역 보기" 링크(`non-pay`→`payment-wait`, 클릭 시 크래시 유발), 주문취소 탭 "삭제" 버튼(`cancle`→`cancel`, 안 뜨던 것).
* **재고 초과 주문건(시스템 오류) 기능 신설** — 디자인 근거: `projects/close/Docs/디자인/{주문내역_재고 예외상황, 초과 주문확인팝업, 초과 주문건 발생_단건팝업}.png`. 백엔드 필드 아직 없어 `src/mock/excessOrderMock.ts`로 프론트 목데이터 우선 구현.
  * 사이드바 "주문내역" 옆 빨간 `!` 배지 (`Admin_menubar.tsx`/`Layout_Admin.tsx`)
  * 대시보드 진입 시 세션당 1회 전체 알림팝업 + "주문내역으로 바로가기" (`Dashboard/Modal/ExcessOrderGlobalModal.tsx`)
  * 결제대기 탭 진입 시 탭 버튼 위에 말풍선 형태 배너(탭 너비와 동일, 꼬리로 탭 가리킴) (`Orderlist.tsx`)
  * 수량 칸에 초과분 빨간 표시 + 클릭 시 단건 팝업 (`Order_paydeil.tsx` + `Modal/ExcessOrderItemModal.tsx`) — PM이 직접 마무리 수정(컬럼 순서, span/Table 전역 CSS 충돌 등)
  * "초과 주문된 주문건 보기" 필터 체크박스 — 결제대기 탭, 초과건 있을 때만 노출
  * `Order_detail`에 `excess_qty?: number` 필드 추가 (`Component/Interface/Order.tsx`)
* `npx tsc --noEmit` 통과 확인.

---

## 4. 🤖 클또리 작업 내역 (fanbird-broadcast — 젬또리 인계 버그 2건 처리)

젬또리가 실기기 E2E 테스트 중 발견해 `memory/inbox.md`로 인계한 치명적 버그 2건 처리.

* **[치명적 버그] 방송 중 뒤로가기 이탈 방지 누락 + 재진입 정합성 오류**: 1차로 `beforeRemove` 가드(이탈 시 "종료하시겠습니까" 확인 후에만 세션 종료)로 처리했으나, PM 실기기 QA 중 "방송종료 버튼이 이미 있으니 뒤로가기는 그냥 목록으로만 보내고 방송은 계속되게 해달라"는 방향 수정 요청 → **최종적으로 beforeRemove 가드는 제거**하고 대신 `IVSBroadcastModule.ts`에 `activeBroadcastSeq` 모듈 변수로 "지금 방송 중인 라이브"를 추적, `BroadcastScreen.tsx`가 마운트 시 이를 보고 `liveStatus` 초기값을 복원(같은 라이브 재진입 시 장치선택 화면이 아니라 방송중 화면으로 바로 복귀)하는 방식으로 전환. 네이티브 세션은 화면 생명주기와 분리된 싱글턴(iOS/Android 둘 다 `teardownIfIdle()`가 방송 중엔 no-op)이라 화면 이탈이 세션에 영향 없음이 확인되어 가능했던 설계. 다른 라이브로 진입 시도 시 충돌 방지 가드도 추가.
* **[잔여 기능] Android 포그라운드 서비스/알림 신설**: 기존엔 관련 코드가 아예 없었음(카메라/마이크 권한만 있고 `FOREGROUND_SERVICE` 계열 권한·서비스 선언 전무).
  * `android/app/.../BroadcastForegroundService.kt` 신설 — "라이브 방송 중" 상시 알림(`NotificationChannel` IMPORTANCE_LOW), API 29+에서는 `FOREGROUND_SERVICE_TYPE_CAMERA|MICROPHONE` 명시.
  * `IvsBroadcastModule.kt`의 `startSession`/`stopSession`에서 서비스 시작/종료 연동.
  * `AndroidManifest.xml`에 `FOREGROUND_SERVICE`/`FOREGROUND_SERVICE_CAMERA`/`FOREGROUND_SERVICE_MICROPHONE`/`POST_NOTIFICATIONS` 권한 + `<service>` 선언(`foregroundServiceType="camera|microphone"`) 추가.
  * `IVSBroadcastModule.ts`의 Android 권한 요청(`requestAndroidPermissions`)에 `POST_NOTIFICATIONS`을 카메라/마이크와 같은 동의 흐름으로 묶어 추가(거부해도 방송 자체는 안 막음 — `granted` 판정에서 제외).
* **검증**: `npx tsc --noEmit`(RN) 통과, `JAVA_HOME=temurin-17 ./gradlew :app:compileDebugKotlin` BUILD SUCCESSFUL 확인.

---

## 5. 🤖 클또리 작업 내역 (fanbird-broadcast — PM 안드로이드 실기기 QA 중 발견분 추가 수정)

PM이 실기기로 직접 검증하면서 나온 후속 이슈들. 전부 처리 완료, 안드로이드 실기기로 검증됨.

* **알림 권한이 요청 안 됨**: 카메라/마이크가 이미 승인된 기기(예전 테스트 기기)에서는 `checkAndRequestAVPermissions`가 AV 권한만 보고 바로 통과시켜, 오늘 추가한 `POST_NOTIFICATIONS` 요청까지 도달을 못 했음. `ensureAndroidNotificationPermission()`을 신설해 AV 권한 상태와 무관하게 방송 시작 시 항상 독립적으로 확인·요청하도록 수정 (`IVSBroadcastModule.ts`, `BroadcastScreen.tsx`).
* **방송 종료 후 라이브목록이 자동 새로고침 안 됨**: `LiveListScreen.tsx`의 데이터 로드가 mount 시 1회만 도는 `useEffect`였는데, 방송화면에서 `navigate('LiveList')`로 돌아오면 스택에 있던 기존 인스턴스를 재사용(리마운트 아님)해서 재실행이 안 됐음. `useFocusEffect`로 교체해 화면 포커스마다 새로고침.
* **"방송중" 라이브를 목록에서 재진입할 방법이 없음**: 액션 버튼이 "예약" 상태에만 노출되던 구조라, 뒤로가기로 나간 뒤 방송중 항목을 다시 못 눌렀음. "방송 화면으로" 버튼을 신설(우측 정렬)해 재진입 가능하게 함 (`LiveListScreen.tsx`).
* **오늘 예약 슬롯 소진 이슈**: 반복 테스트로 오늘 날짜 예약 가능 시간대가 다 막힘. `/unavailable_time`이 종료/취소 상태와 무관하게 슬롯을 막는 것으로 추정, "종료" 상태에도 임시로 취소 버튼을 노출해봤으나(`isTempDeletableEnded`) 백엔드가 "예약" 상태가 아니면 취소 자체를 거부 — 프론트에서 우회 불가로 백엔드 확인 필요 판단.
* **앱 강제종료 시 서버 상태 불일치 확인**: 방송 중 앱을 태스크킬하면 네이티브 세션(실제 송출)은 끊기지만, 정상 종료 API를 호출할 기회가 없어 서버 `live_status`는 "방송중"으로 남는 걸 확인 — 구조적 한계로, 하트비트/AWS IVS 실제 스트림 상태 확인 같은 백엔드 보완이 필요할 수 있음.
* **iOS 백그라운드 한계 재확인**: 앱 내 화면 전환(뒤로가기)은 iOS도 세션이 안 끊기지만(Swift `IVSBroadcastController`도 동일하게 `teardownIfIdle` 방송중 no-op), 앱을 완전히 백그라운드로 보내면 `UIBackgroundModes` 미선언으로 OS가 카메라를 곧 정지시킴 — 기존에 알려진 플랫폼 제약, 오늘 이슈 아님.
* **⚠️ 미원복 임시 변경 2건** (PM이 오늘 회의 데모 위해 유지 요청, 데모 후 원복 필요):
  * `BroadcastScreen.tsx` `TEMP_SKIP_TIME_CHECK = true` — 예약 시간 검증 우회
  * `LiveListScreen.tsx` `isTempDeletableEnded` — 종료 상태 취소 버튼 임시 노출
* **다음 세션**: iOS 실기기로 오늘 수정분 재검증 필요(어제 Archive 했던 빌드엔 오늘 변경 미반영 — 재Archive 필요). `memory/outbox.md` #6에 젬또리에게 추가 QA 요청 남겨둠.

---

## 6. 🤖 클또리 작업 내역 (close 웹 — 주문취소 리뷰 흐름 + 결제완료 탭 + 발주완료 탭)

`Orderlist.tsx` 중심으로 하루 종일 이어진 세 번째 서브 페이즈. 목업 7종(`Docs/디자인/{주문내역_결제취소, 주문취소 여부/완료, 환불 리스트 삭제 여부/완료, 주문내역_발주완료, 발주서 다운, 발주서컬럼, 주문건 미선택 시, 팝업_N차 발주 메모*}.png`) 기준으로 구현.

* **주문취소/환불처리 리뷰 모드 신설**: 항목 체크 → "주문취소" 클릭 → 체크된 항목만 필터링된 리뷰 화면(취소수량 스테퍼 + 택배비 체크 칸만 활성, 나머지 컬럼은 회색 비활성) → "N건 완료하기" → 여부 확인 모달 → 완료 모달. 결제대기 탭은 "주문취소" 문구, 그 외 탭은 "환불처리" 문구로 자동 전환(`isRefundFlow`). 체크 해제 시 즉시 안 빠지고 "환불 리스트 삭제" 확인 모달 경유.
  * 총 결제금액 계산 버그(PM 발견) 수정: `취소수량 × 단가`가 아니라 `(기존수량 - 취소수량) × 단가`가 맞음 — 전량취소 시 0원 확인.
  * UX 재검토로 총결제금액에 **미체크 형제 주문건의 정상가까지 합산**하도록 `fullOrderList` prop 추가.
  * `ConfirmModal.tsx` 신설(여부확인/완료안내 공용 모달), 재사용.
* **CSS 충돌 2건** — 공용 테이블 컴포넌트(`Table`/`MultiRows`, `orderTable_style2.tsx`)의 범용 `span`/`div` 하위선택자가 커스텀 요소로 새는 고질적 패턴. `!important` 오버라이드로 해결(태그 변경 대신 — PM 지정 방식, 반복 확인됨).
* **탭 메뉴 리그레션**: `Order_select_menu.tsx` 테두리 소실 복구 + 자체 스크롤바 완전 제거(페이지 레벨 스크롤에 위임, "짤린다" 피드백 받고 중간 시도 롤백 후 최종 정리).
* **결제완료 탭 상태변경 버튼 커스터마이징**: `statusMenuList`에 "결제대기" 옵션 추가(잘못 바뀐 상태 되돌리기 용도), 결제완료 탭은 환불처리 대신 "사고처리" 버튼 노출.
* **발주완료 탭 신설** (`api-specs.md` #order-batch-list/#order-batch-memo/#purchase-order-download/#purchase-order-upload 신규 계약, 아래 참고):
  * 상단에 "전체 / N차 O명(P건):생성일시" 서브탭 행 신설(`BatchTabRow`) — 클릭 시 해당 회차 detail만 필터링. 서브탭 인원/건수는 `list`를 배치의 `detailSeqs`와 대조해 즉석 산출(`getBatchStats`, 별도 카운트 API 없음).
  * 메모 아이콘: 메모 없으면 연필, 있으면 말풍선(인라인 svg 신규 작성 — 기존 에셋에 없어서). 클릭 시 `OrderBatchMemoModal.tsx`(작성/조회/수정 3단계) 오픈.
  * "발주완료" 상태변경 클릭 시 `submit발주완료`: 그 순간 체크된 detail로 새 회차 자동 생성 + 상태변경 API 호출 + "발주 처리 완료(발주서 다운로드?)" 확인 모달. 확인 시 `purchase_order_download.ts`(xlsx, "발주모아" 컬럼 포맷)로 다운로드 — 상태변경 후 `list`가 reload되어 사라지므로 reload 직전 스냅샷을 별도 보관(`pendingPurchaseOrders`)해서 다운로드에 사용(단순 seq 저장 시 재조회 실패하는 버그를 사전에 잡아 수정).
  * 항목 미선택 상태로 상태변경 버튼 클릭 시 조용히 씹히던 것 → "주문건 미선택" 안내 모달로 전환(`set주문상태` 수정).
  * 우측 버튼 순서: 발주서 다운(보라 아웃라인, 신규 `.outline` 스타일) → 엑셀 업로드(보라 채움, 기존엔 주석처리돼 있던 것 복구) → 엑셀 내보내기(검정, 기존).
  * 발주완료 탭에도 "결제대기" 상태변경 버튼 노출되도록 조건 보정(PM 지적으로 발견).
* **`projects/close/api-specs.md` 신설**(PM 명시 지시 — 프론트가 백엔드보다 먼저 나가는 구간, 필요 API를 진행하면서 계속 정리): 기존 연결된 엔드포인트 8종 표로 정리 + 신규 필요 4건(N차 배치 목록/메모/발주서 다운로드 컬럼 포맷/발주모아 재업로드, 매칭 키 미확정 이슈 명시) 상세 기재. 전역 `memory/api-specs.md`에 인덱스 등록.
* `npx tsc --noEmit` 통과 확인. 브라우저 시각 검증은 PM이 직접 진행 중(발주완료 서브탭/메모모달/발주처리완료 모달 등 체크리스트 전달함).

---

## 📬 Inbox Archive (젬또리 → 클또리, 2026-07-09 작성분)

fanbird-broadcast 실기기 E2E 테스트 중 젬또리가 발견해 인계한 내용 — 처리 완료(위 4번 항목), 원본은 `memory/inbox.md`에서 비움.

**완료 보고**: Android 권한 사전 고지(Prominent Disclosure) 모달 구현 완료 — `BroadcastScreen.tsx`, `Platform.OS==='android'` 분기, "동의 및 허용" 클릭 시에만 네이티브 권한 팝업 연동.

**인계 버그 2건**:
1. [치명적] 방송 송출 중 Android 물리 뒤로가기/스와이프 시 경고 없이 이탈 → 재진입 시 방송중인데 장치선택 대기화면으로 잘못 진입하는 정합성 오류(iOS도 공통 리스크). 수정 가이드: `beforeRemove`/`BackHandler`로 이탈 가로채기 + `stopSession()`/백엔드 상태변경 완결 후 이탈.
2. [잔여] Android 백그라운드 포그라운드 서비스·알림 미작동. 수정 가이드: `IvsBroadcastModule.kt` 포그라운드 서비스/알림 로직 점검, `AndroidManifest.xml` `FOREGROUND_SERVICE` 권한 확인.

관련 파일: `BroadcastScreen.tsx`, `real_device_testing_checklist.md`

---

## 📬 Inbox Archive (젬또리 → 클또리, 배포 전 크래시방어/생명주기 4항목 리서치, 2026-07-09)

`memory/outbox.md` #7 요청에 대한 젬또리 회신(#7). 클또리가 WebFetch로 AWS IVS 공식 문서 원문 대조 검증까지 완료 — 원본은 `memory/inbox.md`에서 비움.

* **Q1 (블루투스 오디오 자동 라우팅)**: SDK가 세션 중 블루투스 연결/해제 자동전환을 보장하지 않음 — 앱이 `AVAudioSession.routeChangeNotification`(iOS)/`AudioDeviceCallback`(Android) 감지 후 수동으로 `IVSDeviceDiscovery` + attach 필요. (원문 직접 대조는 안 했으나 코드 현황과 정합적)
* **Q2 (백그라운드 placeholder) — ✅ 클또리 원문 검증 완료**: `IVSBroadcastSession`에 실제로 `-createAppBackgroundImageSourceWithAttemptTrim:OnComplete:` 메서드 존재 확인(공식 API 레퍼런스 원문 대조). 앱이 백그라운드 진입 시 SDK가 자동으로 placeholder 이미지로 전환해주는 네이티브 지원 API — 직접 route 감지/프레임 스왑 안 만들어도 됨. 반대쪽은 `-removeImageSourceOnAppBackgroundedWithError:`.
* **Q3 (자동재연결 "최대 5회, 선형 백오프") — ❌ 원문 불일치, 폐기**: `IVSBroadcastAutoReconnectConfiguration` 공식 문서 원문 대조 결과 프로퍼티는 `enabled`(Bool, 기본 false) 단 하나뿐. 재시도 횟수·backoff 전략을 문서화/설정하는 API 없음 — "최소 5회 이상" 요구사항과 숫자가 너무 깔끔히 맞아떨어져 의심했는데 실제로 근거 없는 환각 답변으로 확인됨. **→ 실기기 테스트로 실측 필요**(문서로는 검증 불가).
* **Q4 (메모리 누수, stop vs release)**: `stop()`은 캡처 리소스 안 풀고 스트림만 중단, `release()`(Android)/nil 대입(iOS ARC)이 실제 해제 — 기존 코드(`IvsBroadcastController.kt`의 `teardownIfIdle()`)와 이미 정합적.

**다음 조치**: Q2부터 iOS 구현 착수(우선순위). Q3는 코드로 못 고치는 부분이라 배포 전 실기기 5회+ 강제 네트워크 차단 테스트로 검증 예정.
