---
date: 2026-07-07
projects: [close]
tags: [daily]
---

[[Daily]] | [[260706]] ← →

# 작업일지 — 2026-07-07

## 오늘 한 일

### [[projects/close]] — fanbird-broadcast(RN 송출앱) UI/UX 고도화 및 하드웨어 권한 예외 처리 (완료)

*   **하드웨어 권한 거부 대응 및 설정창 이동 로직 구현**:
    *   iOS 네이티브 브릿지([IVSBroadcastModule.swift](file:///Users/linkcampus02/fanbird-broadcast/ios/FanbirdBroadcast/IVSBroadcastModule.swift), [IVSBroadcastModule.m](file:///Users/linkcampus02/fanbird-broadcast/ios/FanbirdBroadcast/IVSBroadcastModule.m))에 `checkPermissions` 및 `requestPermissions` 브릿지 메서드를 신규 작성했습니다.
    *   JS 래퍼([IVSBroadcastModule.ts](file:///Users/linkcampus02/fanbird-broadcast/src/native/IVSBroadcastModule.ts))에서 Android `PermissionsAndroid`와 iOS Swift 브릿지를 단일 인터페이스로 추상화했습니다.
    *   [BroadcastScreen.tsx](file:///Users/linkcampus02/fanbird-broadcast/src/screens/Broadcast/BroadcastScreen.tsx) 진입 및 방송 시작(`handleStart`) 시점에 권한이 최종 거부 상태(`denied`)인 경우, 사용자에게 안내 얼럿을 띄우고 `Linking.openSettings()`를 활용해 **스마트폰의 앱 설정 화면으로 다이렉트 연동**되도록 처리했습니다.
    *   [Info.plist](file:///Users/linkcampus02/fanbird-broadcast/ios/FanbirdBroadcast/Info.plist) 내 `NSCameraUsageDescription` / `NSMicrophoneUsageDescription` 문구를 가이드라인 준수를 위해 상세하게 정교화했습니다.
*   **실시간 ON AIR 노출 상품 팝업 배너 구현**:
    *   화면 진입 시 `is_visable_product` API를 호출하고, 소켓 연결 내에 `Stock` / `Stock Over` 이벤트를 등록하여 실시간으로 방송 상품 정보(이름, 가격, 이미지, 상품번호, 재고)를 동기화했습니다.
    *   채팅창 바 바로 위에 absolute 좌표로 상품 배너 카드가 렌더링되도록 구현했습니다.
*   **하단 밀착 채팅창 및 상품 배너 레이아웃 정합성 개선**:
    *   하단 시스템 네비게이션바/홈 바 가림 방지를 위해 [ChatPanel.tsx](file:///Users/linkcampus02/fanbird-broadcast/src/components/Chat/ChatPanel.tsx) 하단에 동적 safeArea 패딩 뷰를 삽입하고, [BroadcastScreen.tsx](file:///Users/linkcampus02/fanbird-broadcast/src/screens/Broadcast/BroadcastScreen.tsx)의 `chatBox` 전체 높이를 `350`으로 확장해 대화 내용 시독성을 넓혔습니다.
    *   렌더링 순서(Z-Index)를 스왑하여 상품 배너 카드가 항상 채팅창 위 레이어에 오도록 교정했습니다.
    *   `FlatList` 영역 자체를 상품 팝업이 노출될 때는 동적 마진(`style={{ marginBottom: isProductVisible ? 96 : 0 }}`)으로 물리적 차단하여, 채팅 메시지가 상품 배너와 절대 겹치지 않고 바로 위에서만 흐르도록 겹침 버그를 해결했습니다.
*   **동일 명칭 장치 식별자 부여**:
    *   이름이 동일한 카메라/마이크 장치가 잡힐 때 구분하기 쉽도록 URN ID의 끝 4글자(`shortId`) 및 카메라 전/후면 정보를 표시 이름 뒤 괄호에 동적으로 추가 가공해 노출하도록 패치했습니다.

### [[projects/close]] — 웹 셀러 대시보드 기능 고도화 (완료)

*   **고객 게시판(Customer Board) 메뉴 추가**:
    *   [Admin_menubar.tsx](file:///Users/linkcampus02/close/src/Component/UI/Admin_menubar.tsx)에 지정 아이콘 `ic_menu_customer_board.png`를 매핑한 신규 메뉴 항목을 코인 관리와 설정 사이에 신설하고, `/admin/customer-board` 경로로 연동했습니다.
    *   [CustomerBoard.tsx](file:///Users/linkcampus02/close/src/Page/Manage/CustomerBoard/CustomerBoard.tsx) 레이아웃 컴포넌트를 신규 제작하고 [App.tsx](file:///Users/linkcampus02/close/src/App.tsx) 라우터에 바인딩을 완료했습니다.
*   **셀러 자동 로그인(Auto-Login) 기능 구축**:
    *   [AdminLogin.tsx](file:///Users/linkcampus02/close/src/Page/Manage/AdminLogin/AdminLogin.tsx) 로그인 폼에 styled-component 기반의 **자동 로그인 체크박스 UI**를 제작하고 상태관리를 연결했습니다.
    *   로그인 완료 시 **자동 로그인 여부**에 따른 브라우저 보관 분기 처리를 구현했습니다:
        *   자동 로그인 체크 시: `localStorage`에 토큰 및 권한 보관 (브라우저를 닫아도 반영구적 유지)
        *   자동 로그인 미체크 시: `sessionStorage`에 임시 보관 (브라우저를 닫으면 즉시 만료)
    *   [useToken.tsx](file:///Users/linkcampus02/close/src/Component/Context/useToken.tsx) 토큰 훅 및 [api.tsx](file:///Users/linkcampus02/close/src/Component/axios_Interceptor/api.tsx) API 인터셉터가 `localStorage`와 `sessionStorage` 양측의 만료 주기 및 토큰 값을 투명하게 교차 검증하고 파괴하도록 구조 설계를 마쳤습니다.
*   **주문/배송 현황 카드 2열 그리드 리뉴얼 및 신규 상태 연동**:
    *   [Dashborad.tsx](file:///Users/linkcampus02/close/src/Page/Manage/Dashboard/Dashborad.tsx)의 주문/배송 현황 영역을 기획에 따라 **가로 2열(줄) 구조**로 재배치했습니다:
        *   **1열(상단)**: 결제대기 ➔ 결제완료 ➔ 발주완료 (신설) ➔ 배송중 (명칭 변경) ➔ 배송완료 (총 5개)
        *   **2열(하단)**: 주문취소 ➔ 환불처리 ➔ 사고처리 (신설) (총 3개)
    *   신규 아이콘 파일인 `ic_info_order.png`와 `ic_info_accident.png`를 이미지 컴포넌트에 연동하고, 각 상태에 부합하도록 `status_발주완료`, `status_배송중`, `status_사고처리` 변수 및 스위치 케이스 응답 파싱 데이터를 연결했습니다.
    *   `InfoRow` 내부에서 타 구조물(Divider 등)의 스타일이 간섭받지 않도록 `RowView` 상속을 끊고 `styled.div`에 직접 가로 정렬(`justify-content: center`)을 매핑했으며, **`box-sizing: border-box`를 명시하고 1열의 고정 좌우 패딩을 걷어내어** 부모인 `DeliveryInfo` 영역 밖으로 카드가 밀려 삐져나가는 현상을 근본적으로 차단했습니다.
    *   2열의 경우, 숨겨진 더미 뷰들을 완전히 제거하고 **실제 3개 항목만 둔 채 가로폭을 `width: 60%; margin: 0.5rem auto;` 비율로 제어**하여 1열 하단 중앙에 안전하고 안정감 있는 콤팩트 밸런스로 안착되도록 정합성을 최종 픽스했습니다.
    *   건수 수치가 **`0`건일 때 노출되는 비활성 폰트 컬러를 지정해주신 `#A9A9AB` 연회색**으로 전체 일괄 매핑 교정했습니다.

### [[projects/close]] — 웹 라이브 목록 고도화 (완료)

*   **라이브 상태 조건부 폰트 색상 및 최신순 정렬 적용**:
    *   [ManageLivelist.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/ManageLivelist.tsx)의 상태명 렌더링에 삼항 연산자를 활용한 인라인 스타일을 입혔습니다:
        *   `예약` 상태일 경우: 하늘색 **`#5CA6FF`**로 텍스트 출력
        *   `방송 중` 상태일 경우: 빨간색 **`#EB003B`**로 텍스트 출력
        *   그 외 상태: 브라우저 기본 상속 색상(`inherit`) 적용
    *   라이브 목록 조회 API 파라미터의 정렬 조건값을 `sort: "ASC"`에서 **`sort: "DESC"`**로 변경하여, 최신순(제일 최근 예약일시가 맨 위로 올라오도록)으로 정렬 기준을 교정했습니다.

### [[projects/close]] — 웹 라이브 만들기 기능 고도화 (완료)

*   **연속 2일 예약 기능 및 지능형 날짜/시간 UX 연동 구현**:
    *   [Calendar.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Component/Calendar.tsx)의 `livedate` 타입을 `string[]`로 리팩토링하고, 일반적인 예약 서비스 로직에 맞게 날짜 선택 로직을 개선했습니다:
        *   **시간 미선택 상태**에서 연속된 익일(예: 8일)을 누를 경우 ➔ 기존 날짜(7일) 선택이 해제(취소)되고 8일이 시작 날짜로 갱신됩니다.
        *   **시간 선택 상태**에서 연속된 익일(예: 8일)을 누를 경우 ➔ 기존 선택 시간이 유지된 채 7일과 8일이 다중 선택되는 2일 연속 모드로 확장됩니다.
    *   시간 목록 상단의 불필요한 날짜 탭 버튼 UI(`TabRow`)를 완전히 제거하고, 대신 직관적으로 **달력의 날짜 클릭 이벤트를 매핑**하여 `focusDateIdx` 포커스가 전환(예: 달력에서 7일을 누르면 1일차 시간대 목록이 뜨고, 8일을 누르면 2일차 시간대 목록이 노출)되도록 보완했습니다. 2일 연속 모드 돌입 시에도 자동으로 2일차 날짜 시간대가 뜨도록 연동했습니다.
    *   1일차와 2일차 각각의 날짜별로 겹치거나 불가능한 예약을 걸러내기 위해 `unavailableTimeSecond` 상태를 추가해 교차 API 통신 조회를 지원하도록 고도화했습니다.
*   **메뉴 헤더 뒤로가기 버튼 추가 및 정렬 교정**:
    *   [LiveMake.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/LiveMake.tsx) 상단의 '라이브 만들기' 제목 영역 왼쪽에 **뒤로가기 화살표 아이콘(`<`)을 신설**했습니다.
    *   `TitleArea`가 상속하는 `RowView`의 `space-between` 분산 레이아웃으로 인해 글자와 아이콘이 양끝으로 튕겨나가지 않도록, 내부 요소를 단일 `RowView2`로 감싸 **왼쪽 밀착 가로 정렬** 상태로 정합성을 교정하고 클릭 시 목록 페이지(`/admin/livelist`)로 복귀하도록 연동했습니다.
*   **주문 취소 쿠폰 UI 구성 및 API 연동**:
    *   [LiveMake.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/LiveMake.tsx) 썸네일 아래에 styled-component를 이용한 주문 취소 쿠폰 컨트롤러(`[수량] [-] [+]` Stepper UI)를 제작하고 `cancel_ticket` 상태를 연결했습니다. 수량 입력란이 왼쪽에 오고, 가산/감산 버튼이 우측에 붙는 형태이며 가로 너비가 라이브 제목 인풋창과 일치하도록 확장했습니다.
    *   [CoinPaymentModal.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Modal/CoinPaymentModal.tsx) props로 `cancel_ticket`을 연동하고, 라이브 등록(`createLive`) API 페이로드에 이 값을 담아 전송하도록 하드코딩 `0` 값을 정상 대치했습니다.
*   **예약 완료 전 최종 정보 요약 구분선 구현**:
    *   예약 버튼 위에 `SummaryBox` 요약 컴포넌트를 설계하고 `getFormattedSummaryDate` 계산기를 도입하여, 판매자가 선택 완료한 **최종 예약일시(YYYY.MM.DD HH:MM ~ YYYY.MM.DD HH:MM 및 방송 시간)**와 **시청자수** 정보가 한눈에 요약되어 표기되도록 가공했습니다.
    *   별도의 박스 배경 없이 상단에 회색 막대 구분선(`border-top: 1px solid #e8e8e8`)만 얹어 경계를 구분하고, 예약일시 내의 **총 방송 시간 `(X시간)` 표시 부분을 연회색 폰트(`#8E8E8E`) 및 기본 두께(`normal`)로 분기 처리**하여 디자인 정합성을 최종 픽스했습니다.
*   **결제 모달 다중 날짜(2일) 정합성 연동**:
    *   [LiveMake.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/LiveMake.tsx)에서 결제 모달 호출 시 `livedate` props를 단일 문자열(`livedate[0]`)이 아닌 배열 전체(`livedate`)로 전달하도록 수정했습니다.
    *   [CoinPaymentModal.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Modal/CoinPaymentModal.tsx)의 `livedate` props 인터페이스 타입을 `string[]`로 선언 변경했습니다.
    *   모달 렌더링부에서 날짜 수가 2개 이상일 때 **`YYYY.MM.DD ~ YYYY.MM.DD` 포맷으로 예약 일시 범위가 전부 노출**되도록 JSX 분기 구조를 개선하고, API 페이로드에 시작/종료 일시가 2일에 걸친 시간 값으로 정상 전달되도록 매핑했습니다.
*   **라이브 상세페이지 내 주문 취소 쿠폰 정보 표시**:
    *   [Detail_itemList.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Component/Detail_itemList.tsx)에 마운트 시 라이브 전체 정보(`get_live_info`)를 조회하는 로직을 추가하여 해당 예약에 설정된 주문 취소 쿠폰 수량(`cancel_ticket`) 데이터를 불러왔습니다.
    *   시작톡 보내기 등의 액션 버튼 하단에 **취소 쿠폰 정보 영역(`CouponSection`)**을 신설하여 일반 인풋박스와 동일한 흰색 배경의 입력창(`CouponInput`)에 단위(`개`)가 빠진 순수 숫자값을 렌더링하고, 박스 너비를 `6rem`으로 숫자에 맞추어 아주 조밀하게 좁혔습니다. 또한 항목명은 `취소 쿠폰`으로 간소화하고 `readOnly` 속성을 추가해 편집을 차단했으며 하단에 빨간색 글씨로 **`*현재는 수정할 수 없습니다.`** 캡션을 띄워 정합성을 보완했습니다.
*   **상품 등록 컴포넌트(Detail_itemInsert) 레이아웃 및 UX 개편**:
    *   [Detail_itemInsert.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Component/Detail_itemInsert.tsx)의 지난 상품 불러오기 드롭다운을 **독립 버튼화**하고 가로 너비를 절반으로 설정했으며, 기존 하단에 있던 엑셀 업로드 버튼을 그 옆으로 수평 정렬시켜 일체화했습니다. 두 버튼 내부에 각각 신규 제공된 **`ic_refresh.png`** 및 **`ic_upload.png`** 아이콘을 수평 flex 정렬에 맞추어 연동했습니다.
    *   지난 상품 불러오기 버튼을 누르면 드롭다운 대신 **지난 방송 목록 팝업 모달창(`ModalOverlay`)**이 띄워지도록 팝업 설계를 보완하고 복잡한 마우스 리스너 로직을 걷어냈습니다.
    *   상품 등록 폼의 1번째 입력 필드인 `번호`를 **`상품코드`**로 명칭을 수정하고, 기존의 하단 업로드 버튼들을 제거한 뒤, **`추가` 버튼을 입력 폼 하단 행(아래열)에 단독으로 배치**했습니다.
    *   `재고` 입력 필드 우측에 **`상품설명`** 칸을 신설하고 하단에 **`작성하기`** 버튼을 배치했으며, 클릭 시 상품의 상세 텍스트 설명을 작성하고 보관할 수 있는 **텍스트 입력 모달창**을 팝업 설계하고 상품 생성 API 파라미터(`description`)에 연동되도록 매핑을 마쳤습니다.
    *   상품 등록 버튼 윗열에 최대 10개까지 업로드 가능한 **상품 다중 이미지 업로더 영역(`PhotoUploadSection`)**을 신설하여, 레이블 명칭을 실시간 갱신되는 **`사진 (현재개수/10)`** 형태로 변경하고, 비어있는 추가 상자에는 **`ic_plus_gray.png`** 아이콘을 배치했습니다.
    *   등록한 이미지 썸네일 위의 삭제 버튼에는 **`ic_x_circle_white.png`** 원형 아이콘을 장착하여 사용성을 강화했습니다. 또한 등록 폼 및 추가 버튼 아래에 하단의 상품 목록 리스트와 명시적인 시각 구분을 지어주기 위해 가로폭 100% 크기의 **회색 구분선(`Divider`)**을 삽입하고, 등록 버튼 행을 **우측 정렬(`justifyContent: "flex-end"`)**로 마감했습니다.
*   **상품 목록(Detail_itemList) 테스트용 목데이터 폴백 제공**:
    *   [Detail_itemList.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Component/Detail_itemList.tsx) 외부 스코프에 프리미엄 린넨 셔츠, 세미와이드 슬랙스, 데일리 맨투맨티 3종의 **테스트용 상품 목데이터 배열(`mockProductList`)**을 신설했습니다.
    *   API 서버에 등록된 상품 목록이 존재하지 않거나 호출 실패 에러가 발생한 상황에서도 화면 테스트가 용이하도록, 목록 로드 함수(`load_productList`) 실행 시 목데이터로 자동 백업/폴백되도록 비즈니스 로직을 보완했습니다.
    *   상품 검색창에 검색어 한 글자씩 입력할 때마다 실시간으로 화면의 상품 리스트가 걸러지도록 로컬 리스트 필터링 기능(`setting_searchword`)을 유연하게 추가했습니다.
    *   [Live.tsx](file:///Users/linkcampus02/close/src/Component/Interface/Live.tsx)의 `Live_itemList` 인터페이스를 확장하고, 상품목록을 디자인 파일 `상품목록.png` 규격에 정확하게 매치시켜 컬럼 순서를 **`상품코드` ➔ `상품명` ➔ `이미지` ➔ `가격` ➔ `재고` ➔ `남은재고` ➔ `옵션 사용여부` ➔ `옵션` ➔ `상품설명` ➔ `상품노출`** 구조로 전면 조정했습니다.
    *   이미지 칼럼에 상품 썸네일 미리보기와 플러스 클릭 업로드 기능을 배치하고, 상품설명 필드 유무에 따라 **`작성하기`**(보라색) 및 **`수정하기`**(회색) 버튼 분기 처리와 모달창 팝업을 연동했습니다. 10개 열 배치에 어울리도록 정렬 가로 배분을 정밀 조율했습니다.
    *   상품 목록 테이블 내의 `옵션` 열 정렬을 **중앙 정렬(`center`)**로 통일 조율했습니다.
    *   상품노출 상태변경(`setting_product_status`) 시 기존에 `filter` 오작동으로 인해 선택된 1개 행만 남고 나머지 상품들이 실시간 화면에서 사라지는 렌더링 증발 버그를 전수 조사하여 수정했습니다. 또한 요구조건에 맞추어 상태 셀렉트 박스 조작 시 별도의 저장 버튼 클릭 없이 즉시 백엔드 API를 강제 호출해 실시간 갱신되도록 조치했으며, 판매노출 중복 시 기존의 차단 경고창(`alert`)을 띄우는 대신 **기존에 판매노출 중이었던 상품을 자동으로 "판매" 상태로 스왑 강등**시키고, 새롭게 선택된 상품만 유일한 "판매노출" 상태가 되도록 자동 정착 교체 메커니즘을 적용했습니다.
    *   수치 입력용 `InputBox`에 호버/포커스 시 노출되던 브라우저 고유의 숫자 증감 스피너(화살표)를 제거하고, `가격`과 `재고` 입력창의 최대 너비를 제한하여 입력 상자 사이의 가독성 있는 여백을 대량 확보했습니다. 또한 테이블의 모든 열(`상품코드`, `상품명` 등 포함)을 수평 중앙 정렬(`center`)로 최종 통일 마감했습니다.
    *   상품 목록 내의 물품들 사이에 존재하던 경계 디바이더 선(`TableRow`의 `border-bottom`)을 지워 화면을 깨끗하게 정리했고, 이미지 미등록 시 나타나는 플러스 추가 버튼(`EmptyImgBox`) 모양을 원형(`border-radius: 50%`)으로 고도화했습니다.
    *   원형 추가 상자(`EmptyImgBox`)의 배경색을 **`#FBFBFB`**, 테두리 선 색을 **`#EBEBEB`**로 정밀 튜닝하고, 내부에 얹어진 플러스(`+`) 아이콘의 크기를 **`1.6rem`**으로 키워 정착시켰습니다.
    *   수정사항 검사식(`isNotModi`)에서 변경 감지 대상인 `product_status`(노출 상태)와 신규 필드인 `description`, `product_img` 대조가 누락되어 수정이 일어나도 저장 버튼이 계속 비활성화(회색) 상태로 유지되던 오작동 버그를 발견하여, 편집 가능한 모든 필드(가격, 재고, 노출상태, 이미지, 설명, 옵션여부)를 정확하게 1:1로 비교하도록 핫픽스하여 값이 바뀔 시 즉시 활성화(보라색)로 연동되게 교정했습니다.
    *   상품 목록 테이블 하단의 데이터 없음 안내(`Nonedata`) 구문을 로직 분기하여, 아예 등록된 상품이 하나도 없는 경우에는 **`"등록된 상품이 없습니다."`**를 출력하고, 상품은 있으나 검색 조건에 매칭되는 결과가 비어있는 경우에는 **`"검색한 상품을 찾을 수 없습니다."`**로 명확하게 나뉘어 가이드하도록 개선했습니다.
    *   모달 전용 디렉토리 `Modal/` 하위에 요구되는 3개 모달 컴포넌트를 설계에 어울리는 프리미엄 UI로 구현하여 추가했습니다:
        1. [LoadPastProductModal.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Modal/LoadPastProductModal.tsx): 지난 방송 목록을 라디오 버튼으로 1개 선택해 상품 리스트를 로드할 수 있도록 달력 필터와 수직 리스트를 적용해 연동했습니다. 특히 날짜 입력창을 HTML5 `date` 타입으로 변경한 뒤, 사용자가 입력 박스의 어느 영역을 눌러도 네이티브 데이트피커(달력 팝업)가 구동되도록 `showPicker()` API 트리거 및 기본 웹킷 달력 아이콘 숨김 CSS를 적용했습니다.
        2. [ProductImageModal.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Modal/ProductImageModal.tsx): 이미지 목록을 조회하고 삭제/추가할 수 있도록 스크롤 썸네일 구조와 함께 최대 10개 업로드 제한을 두어 목록 썸네일 클릭 시 팝업되도록 바인딩했습니다. 추가로, 스크롤 이미지 컨테이너(`ImagesListContainer`) 및 이미지 부재 시 노출되는 영역(`EmptyState`)의 높이를 최대 **`26rem`** 및 최소 **`16rem`**으로 대폭 확장하였으며, 기존 이미지 부재 상태에서 모달에 진입했을 경우 하단 저장 버튼이 수정 완료가 아닌 **`"등록 완료"`**로 동적 변경되도록 분기 조건(`initialHasImage`)을 설정했습니다. 또한, 추가로 업로드한 사진이 대표 이미지 썸네일로 즉시 정착되도록 배열의 맨 앞(index 0)에 병합되게 순서 동기화를 패치했습니다. 그리고 단일 대표 이미지 외에 다중 이미지 목록의 정보 유실을 차단하기 위해 `Live_itemList` 인터페이스에 **`product_imgs` (문자열 배열)** 속성을 신설하고, 부모 컴포넌트의 React State와 모달 저장/로드 간 데이터 바인딩을 매칭하여 여러 이미지를 추가한 뒤 모달을 다시 열어도 업로드된 모든 이미지 목록이 메모리 유실 없이 완벽 복원되도록 처리했습니다.
        3. [ProductDescriptionModal.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Modal/ProductDescriptionModal.tsx): 상품설명의 유무에 따라 버튼 구조가 분기되고(작성 완료 단일 vs 수정 완료/삭제 2열), 삭제 클릭 시 '2차 확인' 및 '3차 삭제 완료' 팝업까지 흐르도록 설계해 등록 폼 및 목록 테이블에 일제히 접목했습니다. 또한 요구조건에 맞추어 메인 모달의 헤더 제목을 **정중앙 정렬**하고 닫기 아이콘 위치를 절대배치로 정밀 제어한 뒤, 제목 바로 아래에 가로폭 100% 크기의 **회색 구분선 디바이더(`HeaderDivider`)**를 추가해 가독성을 높였습니다. 추가로 판매자가 긴 설명 텍스트를 여유롭게 기입하고 검토할 수 있도록 텍스트에어리어 입력창 세로폭(`height`)을 기존 `14rem`에서 **`22rem`**으로 대폭 늘렸습니다.
        4. 신규 모달 3종([LoadPastProductModal.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Modal/LoadPastProductModal.tsx), [ProductImageModal.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Modal/ProductImageModal.tsx), [ProductDescriptionModal.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Modal/ProductDescriptionModal.tsx)) 전체의 헤더 영역 레이아웃에 **제목 텍스트 정중앙 정렬(`center`)** 및 닫기 버튼 절대배치 수정을 균일하게 완료하고, 제목 아랫선에 가로폭 100%의 **회색 디바이더 경계선(`HeaderDivider`)**을 일괄 주입하여 시각적 정합성을 완성했습니다.
        4. 신규 모달 3종 컴포넌트 상단에서 공통 레이아웃(`style` 경로 대소문자 매칭 오기) 및 폰트 스타일 모듈(`font` -> `common_font`)의 경로 참조를 정상화하여 웹팩 컴파일 에러를 일괄 해결했습니다.
        5. 신설된 모달들에 들어가는 주 보라색 버튼들(`불러오기`, `수정 완료`, `작성 완료`, `확인` 등)의 배경색을 시작톡 보내기 버튼과 완벽히 호환되도록 그라데이션 스타일(`linear-gradient(135deg, #7955f0, #3209b9)`)로 리셋 튜닝했습니다.
        6. 시작톡 발송 유저 수 및 결제 확인 모달([SendPaymentModal.tsx](file:///Users/linkcampus02/close/src/Page/Manage/LiveList/Modal/SendPaymentModal.tsx)) 헤더 타이틀에서 백엔드 통신 상태인 `"알림톡"` 분기 문자열은 온전히 유지하면서 화면에 보이는 명칭만 **`"시작톡 보내기"`**로 실시간 맵핑 출력되도록 안전하게 변환했습니다.
