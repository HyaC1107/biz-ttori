# triggers.md — 비즈니스 협업 호출 트리거 규칙

이 문서는 클또리가 사내 업무 및 개발(현재 AI 모델 개발 중심, 세부 스코프 미정) 과정에서 어떠한 기준으로 하위 에이전트(Sub-agents) 또는 동생봇(젬또리, 챗또리)들을 호출할지 규정합니다.

---

## 1. 🚦 AI 호출 트리거 매핑

| 조건 / 상황 | 호출 대상 | 방식 및 명령어 |
|:---|:---|:---|
| **독립된 개발 태스크 분담** | `coder` 에이전트 | Agent Spawn (모듈/기능 단위 개발) |
| **코드 PR 전 검토 & 엣지케이스** | `reviewer` 에이전트 | Agent Spawn (diff 한정, 작성 분신과 분리) |
| **코드 최종 리뷰 & 보안 감사** | 린터/타입체커/테스트 → `reviewer` 분신 → (필요 시) `coderabbit` | 결정론적 도구 선통과 후 LLM 리뷰. cross-model 보강은 coderabbit 플러그인 |
| **사양서/기획서 설계 초안 작성** | `planner` 에이전트 | Agent Spawn (아이디어 수집 및 뼈대 설계) |
| **최신 모바일/웹 기술 리서치** | 👤 PM이 직접 젬또리 실행 (T1) | `agy -p "[리서치 키워드]"` → 요약본 주입 |
| **문서 팩트체크 및 수치 검증** | 👤 PM이 직접 젬또리 실행 (T1) | `agy -p "[문서 검증 내용]"` → 요약본 주입 |
| **개발 일지 및 Slack 공유본 작성**| `writer` 에이전트 | Agent Spawn (로그 요약 및 슬랙 알림 포맷) |

---

## 2. 🛡️ 개발 단계별 검증 규칙 (PR 게이트)

사내 프로젝트 및 기획 문서 배포 전, 아래 기준에 맞춰 교차 검증을 반드시 통과해야 합니다.

> ⚠️ 아래 항목은 AI 모델 개발 업무 범위가 아직 확정되지 않아 임시 자리표시자다. 실제 업무(예: 데이터 파이프라인, 학습/평가 스크립트, 모델 서빙 등)가 정해지면 구체적인 대상으로 교체한다.

```
🔴 강화 검증 (결정론적 도구 → reviewer 분신 → coderabbit)
- 데이터/모델 파이프라인 인터페이스(계약) 변경
- 학습 설정·하이퍼파라미터·평가지표 산출 로직 변경 (결과 신뢰도에 직결)
- 인증/보안, 자격증명 관련 코드
- 빌드/배포 설정 파일 수정 (package.json, 의존성 lock 파일 등)
- 절차: ① 린터/타입체커/테스트 선통과 → ② reviewer 분신 adversarial 리뷰 → ③ coderabbit cross-model 보강

🟡 reviewer 분신 리뷰로 충분
- 비즈니스 로직이 들어가지 않는 단순 유틸리티 함수
- 문서 템플릿 변경

🟢 교차 리뷰 생략 가능
- 단순 오타 수정 및 마크다운 주석 업데이트
- daily/ 일지 작성
```

> ⚠️ **챗또리(codex) 보류 중.** 위 🔴는 원래 codex 외부검증 자리였으나, 현 2인 체제에선 **결정론적 도구(린터/타입/테스트)를 LLM 리뷰 앞단에 먼저** 통과시키고 `coderabbit`(cross-model)으로 보강한다. codex 재도입 시 🔴를 codex 필수 리뷰로 승격.

---

## 🗣️ Debate(토론) 패턴 발동 기준

다음과 같이 결정을 내리기 어려운 주제는 **다관점 토론(Debate)**으로 결론을 도출합니다:

1. **핵심 라이브러리/프레임워크 선정** (예: 학습 프레임워크, 상태 관리 라이브러리 등 — 업무 확정 후 구체화)
2. **인프라/배포 트레이드오프** (예: 로컬 vs 클라우드 학습, 서빙 방식 등 — 업무 확정 후 구체화)
3. **데이터/모델 설계 변경** (스키마·인덱싱, 또는 데이터/모델 파이프라인 구조 변경)
4. **기획/사업 방향 결정** (기능의 MVP 포함 여부 및 D-day 산정)

**현 2인 체제 진행 방식** (챗또리/codex 보류):
- 👤 PM이 직접 젬또리(`agy`)를 실행(T1)해 외부 관점 요약본을 가져오고,
- 클또리는 내부 분신을 **서로 다른 페르소나(예: MVP우선 vs 리스크우선)**로 병렬 띄워 대립 의견을 만든 뒤,
- 두 입력을 종합해 PM에게 승인 요청.

> codex 재도입 시 → 젬또리(agy) + 챗또리(codex) **cross-model 동시 호출**로 승격(진짜 독립 모델 토론).
